import { FC } from 'react';
import styles from './Cart.module.scss';

const Cart: FC = () => {
  return (
    <div className={styles.wrapper}>
      <h2>Cart Page</h2>
    </div>
  );
};

export default Cart;
