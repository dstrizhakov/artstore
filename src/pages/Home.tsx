import { FC } from 'react';
import Button from '@mui/material/Button';

const Home: FC = () => {
  return (
    <div>
      <h1>Home Page</h1>
      <Button variant="contained">Contained</Button>
    </div>
  );
};

export default Home;
